# Expo / React Native + Metro cannot resolve pnpm's symlinked store reliably.
# Hoisted node_modules is the officially supported layout for Expo in a pnpm monorepo.
node-linker=hoisted
shamefully-hoist=true
strict-peer-dependencies=false
auto-install-peers=true
